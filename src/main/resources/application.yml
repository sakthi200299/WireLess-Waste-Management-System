spring:
  application:
    name: WireLess-Waste-Management-System

  datasource:
    url: jdbc:h2:mem:waste_db
    driver-class-name: org.h2.Driver
    username: SUNWARE37
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  h2:
    console:
      enabled: true

server:
  port: 8081
